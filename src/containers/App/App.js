import React from 'react';


import Router from '../../router/Router';


const App = () => (
  <React.Fragment>
    <Router />
  </React.Fragment>
);

App.propTypes = {
  // classes: PropTypes.object.isRequired,
};

export default App;
